# Copy to .env and fill in values
# For local dev with SQLite, most defaults are fine.

# Database (leave as SQLite for simple local dev)
DATABASE_URL=sqlite:///./sprintsync.db

# JWT signing key — change in production!
SECRET_KEY=change-me-to-something-long-and-random

# AI — leave blank to use deterministic stub
OPENAI_API_KEY=

# Force stub even if API key is set (useful for CI)
USE_AI_STUB=false
